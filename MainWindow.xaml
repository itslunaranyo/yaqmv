<Window x:Class="yaqmv.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
        xmlns:local="clr-namespace:yaqmv"
        mc:Ignorable="d"
		Loaded="Window_Loaded"
        Title="YAQ Model Viewer" Height="720" Width="600">
	<Grid Background="#FFDFDFDF">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="180" />
		</Grid.RowDefinitions>
		<Menu VerticalAlignment="Top">
			<MenuItem Header="_File">
				<MenuItem Header="_Open" Click="MenuFileOpen" />
				<Separator />
				<MenuItem Header="_Exit" Click="MenuFileQuit" />
			</MenuItem>
		</Menu>
		<local:ModelWindow 
				x:Name="ModelWindow" 
				Render="MWOnRender"
				SizeChanged="MWOnSizeChanged"
				Unloaded="MWOnUnload"
				KeyDown="MWOnKeyDown"
				MouseMove="MWOnMouseMove"
				MouseWheel="MWOnMouseWheel"
				Grid.Row="1"
			/>
		<Grid Grid.Row="2">
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<StackPanel Margin="8">
				<WrapPanel>
					<TextBox Text="Sequence: " Background="{x:Null}" BorderBrush="{x:Null}" />
					<ComboBox x:Name="AnimSelect" 
							  Width="128" 
							  IsReadOnly="True"
							  SelectionChanged="AnimSelect_SelectionChanged"/>
				</WrapPanel>
			</StackPanel>
		</Grid>
	</Grid>
</Window>
